# CLAUDE.md

คำแนะนำสำหรับ Claude เวลาทำงานกับ vault นี้

## Vault นี้คืออะไร

Obsidian knowledge vault ส่วนตัว เก็บโน้ตอ้างอิงทางเทคนิคเป็นภาษาไทย (Quarkus, Java, Docker, CI-CD, Cloudflare, Angular, RxJS, Typescript ฯลฯ) จัดโฟลเดอร์แยกตามหัวข้อ/เทคโนโลยี เช่น `Quarkus/`, `Java/`, `Docker/`

## กฎที่ต้องทำตามเสมอ

### 1. ห้ามโยงกับของบริษัท

เนื้อหาที่ค้นคว้า/เขียนในนี้ต้อง **เป็นความรู้ทั่วไป (generic) เท่านั้น** ห้ามอ้างอิงหรือโยงกับ `cdgs` / `bde-extension` / framework ภายในของบริษัทโดยเด็ดขาด แม้หัวข้อจะเกี่ยวกับเทคโนโลยีที่บริษัทใช้อยู่ก็ตาม (เช่น Quarkus, Keycloak) — เขียนแบบทั่วไปที่ใช้ได้กับทุกโปรเจกต์ ไม่ผูกกับ implementation เฉพาะของที่ทำงาน

### 2. ไดอะแกรม/ผัง ใช้ Mermaid ไม่ใช่ ASCII art — แต่ไม่ใช่ทุกรูปแบบที่เบี้ยว

**กฎจริง ๆ ไม่ใช่ "ห้ามมี box-drawing character" แต่คือ "ห้ามให้ข้อความไทยสองจุดขึ้นไปต้องเรียงคอลัมน์ตรงกัน"** — สระ/วรรณยุกต์ไทยความกว้างไม่เท่าตัวอักษรอังกฤษ เวลา render ใน monospace font ตำแหน่งจะเบี้ยว ไม่ใช่ปัญหาที่แก้ด้วยฟอนต์

**❌ ต้องเลี่ยง → ใช้ ` ```mermaid ` แทน:**
- กล่อง/ผังที่มีข้อความไทย **อยู่ข้างในกรอบ** (`┌│└┐┘├┤┬┴┼`) — กรอบข้างขวาจะไม่ตรงแนว
- ตารางเปรียบเทียบ 2-3 คอลัมน์ที่ข้อความไทยต้องขึ้นต้นตรงตำแหน่งเดียวกันในแต่ละแถว (ใช้ตาราง Markdown จริงแทนก็ได้ ไม่จำเป็นต้องเป็น Mermaid เสมอไปถ้าเป็นข้อมูลเชิงเปรียบเทียบไม่ใช่ flow)
- แผนผัง flow/branch/state ที่มี `│▼├└` เชื่อมระหว่างข้อความไทยหลายก้อน โดยเฉพาะที่แตกสาขา

**✅ ปลอดภัย ไม่ต้องแก้:**
- **file tree** (`├── โฟลเดอร์/` ตามด้วยคอมเมนต์ไทยท้ายบรรทัด) — ชื่อไฟล์เป็นอังกฤษล้วน ข้อความไทยแค่ตามหลังไม่ต้องเรียงคอลัมน์กับอะไร
- **คำอธิบาย command/config แบบมีเส้นชี้** เช่น
  ```
  docker run  -d          -p 8080:80
     │        │             │
     │        │             └── คำอธิบายภาษาไทย
     └── คำอธิบายภาษาไทยอีกอัน
  ```
  รูปแบบนี้ปลอดภัยเสมอ เพราะเส้น `│` อ้างอิงตำแหน่งจาก command ที่เป็นอังกฤษ/สัญลักษณ์ล้วนด้านบน ส่วนข้อความไทยอยู่ท้ายสุดของแต่ละบรรทัด ไม่ต้องเรียงตรงกับอะไรทั้งสิ้น — **ห้ามแปลงรูปแบบนี้เป็น Mermaid โดยไม่จำเป็น** เพราะมันชัดเจนและอ่านง่ายอยู่แล้ว

Obsidian รองรับ Mermaid ในตัวอยู่แล้ว ไม่ต้องลง plugin เพิ่ม, render เป็น SVG จริง

### 3. โครงสร้างโน้ต

- โฟลเดอร์แยกตามเทคโนโลยี/หัวข้อ (ดูโฟลเดอร์ที่มีอยู่เป็นตัวอย่างก่อนสร้างใหม่)
- frontmatter รูปแบบมาตรฐาน:
  ```yaml
  ---
  tags:
    - ...
  type: reference   # หรือ moc สำหรับโน้ต index รวมของโฟลเดอร์ (เช่น Quarkus.md, Java.md)
  created: YYYY-MM-DD
  ---
  ```
- หัวข้อเดียวจบในตัว ไม่ต้องแยกเป็น index + sub-notes เสมอไป — แยกเฉพาะหัวข้อที่มีเนื้อหาสะสมเยอะจริง ๆ (เช่น Quarkus, Java ที่มี MOC รวม)
- ปิดท้ายโน้ตด้วย: กับดัก/ข้อควรระวัง → cheat sheet → cross-link (`[[wikilink]]`) ไปโน้ตอื่นที่เกี่ยวข้อง → แหล่งอ้างอิงที่ค้นมาจริง
