# Secrets — API keys, TLS private keys ของ plugin (Local REST API เก็บ private key ไว้ตรงนี้)
.obsidian/plugins/*/data.json

# ทั้งโฟลเดอร์เป็นโน้ต setup งานบริษัท ไม่เอาเข้า repo นี้เลย
setup project/

# workspace/UI state ของเครื่องนี้ ไม่ควร sync ข้ามเครื่อง
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# ไฟล์ระบบ
.DS_Store
Thumbs.db
